# Claude Code 稼働環境（AWS EC2 / Ubuntu）操作手順書

HCP Terraform（VCS駆動）で EC2 を構築し、SSH / VS Code から Claude Code を使う
までの一連の手順書。プロジェクト識別子: **J32474** / リージョン: 東京(ap-northeast-1)。

---

## 0. この構成で作られるもの
- VPC × 1・パブリックサブネット × 1（新規）
- Ubuntu 24.04 LTS の EC2（t3.medium）＋ Elastic IP（固定外部IP）
- 起動時に user_data で Node.js(LTS) と Claude Code を自動インストール
- SSM Session Manager 用 IAM ロール、セキュリティグループ
- （任意）EC2 定期停止/起動スケジューラ

## 1. 前提
- HCP Terraform のワークスペースが GitHub リポジトリと VCS 連携済み
- ワークスペースの Execution Mode = Remote
- 手元 PC に AWS CLI / Session Manager Plugin（SSM を使う場合）
- SSH 鍵ペアを作成済み（未作成なら 2-1 を実施）

---

## 2. 事前準備（SSH 鍵）

### 2-1. 鍵ペアの作成（未作成の場合のみ / PowerShell）
```powershell
# .ssh フォルダが無ければ作成
New-Item -ItemType Directory -Path "$HOME\.ssh" -Force
# ed25519 鍵を作成
ssh-keygen -t ed25519 -f "$HOME\.ssh\claude-code-j32474" -C "claude-code-j32474"
```
> パスは "~" ではなく "$HOME" を使う（PowerShell は ~ を展開しない）。

### 2-2. 公開鍵の中身を取得
```powershell
Get-Content "$HOME\.ssh\claude-code-j32474.pub"
```
出力される `ssh-ed25519 AAAA...claude-code-j32474` の1行をコピーする。
（秘密鍵＝拡張子なしの方は絶対に外部に出さない・git に上げない）

---

## 3. HCP Terraform 変数の設定

ワークスペースの **Variables** で以下を設定する。

| 種別 | キー | 値 | 備考 |
|------|------|----|------|
| Terraform variable | `ssh_public_key` | 2-2 でコピーした公開鍵1行 | これを設定するとキーペアが作成され EC2 に登録される |
| Environment variable | `AWS_ACCESS_KEY_ID` | （専用IAMユーザーのキー） | **Sensitive を必ずチェック** |
| Environment variable | `AWS_SECRET_ACCESS_KEY` | 同上のシークレット | **Sensitive を必ずチェック** |
| Terraform variable | `instance_environment`（任意） | 例 `{ TZ = "Asia/Tokyo" }` | 永続環境変数。機密は入れない |

> AWS 認証情報・公開鍵の入力はブラウザ上で各自が行う。コードや git には書かない。

---

## 4. リポジトリへの反映（git）

コード変更後、以下をコミット & push する。VCS 駆動なので push で plan が走る。

**コミットに含めるファイル**
- `versions.tf` / `variables.tf` / `main.tf` / `outputs.tf`
- `user_data.sh.tftpl`（★これが無いと templatefile がエラーになる）
- `.gitignore` / `README.md` / `terraform.tfvars.example`

**コミットに含めないもの（.gitignore 済み）**
- `*.tfstate`（機密）/ `terraform.tfvars`（実値）/ `*.pem` などの鍵

```powershell
git add .
git status            # tfstate や実値 tfvars が入っていないことを確認
git commit -m "Enable key pair and templated user_data"
git push
```

> 注: `main.tf` の user_data は `file()` ではなく
> `templatefile("${path.module}/user_data.sh.tftpl", {...})` を参照する。
> `user_data.sh.tftpl` がリポジトリに含まれていることを push 前に必ず確認する。

---

## 5. デプロイ（plan → apply）

1. push すると HCP でトリガーされた **plan** が走る（走らない場合は Runs → New run で手動実行）
2. plan の内容を確認する
   - ⚠️ 稼働中インスタンスに `key_name` を新規付与する場合、
     プランに **`forces replacement`（インスタンス再作成）** と表示される。
     再作成すると EBS 上の手動セットアップは消え、user_data による自動セットアップが走る。
3. 問題なければ **Confirm & Apply** を押して apply

apply 後、接続先 IP は出力で確認:
```powershell
terraform output public_ip   # ローカルCLI併用時。HCP UI の Outputs でも確認可
```

---

## 6. 接続方法

### 6-A. SSH で接続

**OpenSSH（PowerShell）**
```powershell
ssh -i "$HOME\.ssh\claude-code-j32474" ubuntu@<Elastic IP>
```

**TeraTerm**
- Host: `<Elastic IP>` / Service: SSH / Port: 22
- User name: **ubuntu**（← 鍵名ではない）
- 認証: 「RSA/DSA/ECDSA/ED25519 key を使う」→ 秘密鍵 `claude-code-j32474`（拡張子なし）を指定
- ※ ed25519 鍵が「読み込みエラー」になる場合は TeraTerm を最新版(5.x)に更新する

### 6-B. VS Code Remote-SSH

1. 拡張機能「Remote - SSH」をインストール
2. `C:\Users\J32474\.ssh\config` を作成（★拡張子を付けない）
   ```powershell
   @"
   Host claude-code-j32474
       HostName <Elastic IP>
       User ubuntu
       IdentityFile C:/Users/J32474/.ssh/claude-code-j32474
   "@ | Set-Content -Path "C:\Users\J32474\.ssh\config" -Encoding ascii
   ```
   > `HostName` には必ず **数字の Elastic IP** を書く（ホスト名を書くと
   > 「Could not resolve hostname」で失敗する）。
3. まず素の ssh で疎通確認（config が効くか）
   ```powershell
   & "C:\WINDOWS\System32\OpenSSH\ssh.exe" claude-code-j32474
   ```
4. VS Code →「Remote-SSH: Connect to Host」→ `claude-code-j32474`

**接続が「Canceled」/ 不安定なときの対処**
- 設定 `remote.SSH.useExecServer` を **オフ**
- 設定 `remote.SSH.connectTimeout` を **60** 程度に延長
- ssh-agent に鍵を登録してパスフレーズ入力待ちを回避
  ```powershell
  Get-Service ssh-agent | Set-Service -StartupType Manual
  Start-Service ssh-agent
  ssh-add "$HOME\.ssh\claude-code-j32474"
  ```

---

## 7. Claude Code の認証（apply 後・手動）

接続後、プロジェクトディレクトリで起動する。
```bash
cd ~
claude
```
表示されるログイン URL を手元ブラウザで開き、返ってきたコードを貼り戻す（ヘッドレス対応）。

> **Team ライセンスでサブスク認証が使えるかは要確認**（プラン仕様は変わりうる）。
> - プラン/シート/課金: https://support.claude.com
> - 認証手順の最新情報: https://docs.claude.com/en/docs/claude-code/overview

---

## 8. 環境変数を追加したいとき

`instance_environment`（HCP の Terraform variable、または terraform.tfvars）に
キー/値を追加して apply する。例:
```hcl
instance_environment = {
  TZ          = "Asia/Tokyo"
  NODE_OPTIONS = "--max-old-space-size=2048"
}
```
`/etc/profile.d/claude-code-env.sh` に書き出され、次回ログインシェルから有効。
★ API キーやトークンなどの機密は入れないこと。

---

## 9. 手動セットアップ（自動が失敗した場合のフォールバック）

user_data が失敗した／手で流し直したい場合、SSH または SSM で入り、以下を順に実行。
```bash
sudo apt-get update -y
sudo apt-get install -y git curl ca-certificates

curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.1/install.sh | bash
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"
nvm install --lts
nvm alias default 'lts/*'

npm install -g @anthropic-ai/claude-code
node -v
claude --version
```
実行ログの確認: `sudo cat /var/log/cloud-init-output.log`

---

## 10. 定期停止 / 起動（任意）

`schedule.tf` を導入している場合、EventBridge Scheduler が cron で
SSM Automation（AWS-Stop/StartEC2Instance）を実行して停止/起動する。
- 既定: 毎日 21:00 JST 停止 / 平日 09:00 JST 起動
- 停止中も Elastic IP と EBS には課金が発生する（EC2 稼働課金は停止）
- 停止/起動をまたいでも EBS 上の環境（Node・Claude Code）は保持される

---

## 11. 破棄
```powershell
terraform destroy   # ローカルCLI併用時。HCP UI からは Settings → Destruction で
```
Elastic IP は割り当て中ずっと課金対象のため、使わない期間は破棄推奨。

---

## 付録: つまずきやすいポイント早見表

| 症状 | 主な原因 | 対処 |
|------|---------|------|
| plan が走らない | ブランチ不一致 / Execution=Local / Webhook 失敗 | VCS branch・Execution Mode・Webhook配信を確認 |
| `no file exists at ./user_data...` | テンプレートがリポジトリに無い | `user_data.sh.tftpl` を git add して push |
| SSH `Permission denied` | ユーザー名が ubuntu でない / 鍵未登録 | User=ubuntu、authorized_keys 登録を確認 |
| TeraTerm 鍵読み込みエラー | 古い TeraTerm が ed25519 非対応 | TeraTerm を 5.x に更新 |
| VS Code `Could not resolve hostname` | config の HostName がIPでない | HostName に Elastic IP を記載 |
| VS Code `Canceled` | ExecServer 不整合 / タイムアウト | useExecServer オフ・connectTimeout 延長 |
